fun factorial(n) {
    if (n < 0) { return nil; }
    if (n <= 1) { return 1; }
    return n * factorial(n - 1);
}

write("Enter n: ");
var n = read();
writeln("Factorial of " + n + " is " + factorial(n));